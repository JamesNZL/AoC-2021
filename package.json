{
  "devDependencies": {
    "@types/node": "^17.0.4",
    "copyfiles": "^2.4.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "clean": "rimraf dist/",
    "copy-files": "copyfiles -u 1 src/**/*.txt dist/",
    "build": "npm run clean && npm run copy-files && tsc"
  }
}