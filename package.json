{
  "devDependencies": {
    "@types/node": "^17.0.4",
    "copyfiles": "^2.4.1",
    "typescript": "^4.5.4"
  },
  "scripts": {
    "copy-files": "copyfiles -u 1 src/**/*.txt dist/",
    "build": "npm run copy-files && tsc"
  }
}
